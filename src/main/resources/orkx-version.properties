# Filtered by Maven, so `orkx --version` cannot drift from the pom. Everything below the
# version is the attribution the licence's section 7(b) term requires to be kept — see NOTICE.
version=${project.version}
copyright=Copyright (C) ${project.inceptionYear} ${copyright.holder}
licence=${licence.name}, with an attribution term - see NOTICE.
source=${project.url}
